# Dependencies
node_modules/

# Tool caches / generated indexes (not part of the framework distribution)
.gitnexus/

# Skills are installed per-project via the `skills` CLI (reference & auto-install),
# NOT redistributed by hero-vibe-kit (license: they belong to obra/superpowers,
# Leonxlnx/taste-skill, GitNexus — keep them out of this repo):
.agents/
.claude/skills/

# hero-vibe-kit per-project runtime config (created by `init` in a consumer repo)
.hero-vibe-kit/

# OS / editor
.DS_Store
Thumbs.db
*.bak
*.log
